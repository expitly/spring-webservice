h2:
  console:
    enabled: true

logging:
  level:
    org:
      hibernate:
        type: true

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        id:
          new_generator_mappings: false